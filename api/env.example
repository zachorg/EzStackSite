# --- Server ---
# Optional. Defaults to 4000
PORT=4000

# --- Environment ---

# --- API Key generation ---
# Required. Pepper concatenated with plaintext before Argon2 hashing
APIKEY_PEPPER=change-me-strong-secret

# --- Firebase Admin (Service Account) ---
# Option A: Full JSON blob
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"your-project-id",...}

# Option B: Split vars (use literal \n in PRIVATE_KEY)
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nMIIEv...\n-----END PRIVATE KEY-----\n
# Example PRIVATE KEY format (single line with \n):
# -----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n
# --- Google KMS (Optional; only used for demo encryption path) ---
# KMS_KEY_RESOURCE=projects/<project>/locations/<location>/keyRings/<ring>/cryptoKeys/<key>

