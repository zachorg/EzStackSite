rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /apiKeys/{id} {
      allow read: if request.auth != null && resource.data.userId == request.auth.uid;
      allow write: if false; // All writes must go via Cloud Functions/Admin SDK
    }
  }
}

